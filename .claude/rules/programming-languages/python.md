# Python Guidelines

- The code should pass the project linting tool rules (for example, mypy, ruff, etc)
- The code should pass the project formatting tool rules (ruff, etc)
- As a rule of thumb prefer abc.ABC interfaces over Protocol in Python except for simple cases
- Keep imports block sorted following Ruff rules